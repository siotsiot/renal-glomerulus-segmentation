# Recorded production runtime: Python 3.11.9, PyTorch 2.7.1+cu128, CUDA 12.8.
# Install the PyTorch build appropriate for the target platform/CUDA runtime.
torch==2.7.1

# Required by the frozen production source. Exact production versions were not
# recorded in the run manifests, so unsupported exact pins are not asserted.
numpy
opencv-python
timm

